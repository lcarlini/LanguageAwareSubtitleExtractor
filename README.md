# LanguageAwareSubtitleExtractor
A Python script that ingests an .mp4 video, optionally selects the audio language and applies a noise-filtering layer, then uses a VGA-based pipeline to extract and output subtitles as a plain .txt file.
